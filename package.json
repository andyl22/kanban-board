{
  "name": "kanban-board",
  "version": "1.0.0",
  "description": "Package for heroku",
  "scripts": {
	"start": "cd backend && node ./bin/www",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../backend && npm install"
  }
}